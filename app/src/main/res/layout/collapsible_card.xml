<?xml version="1.0" encoding="utf-8"?>
<merge
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:parentTag="android.widget.FrameLayout">

    <!-- Note: putting everything in a RelativeLayout confuses Talkback, which does not always
    focus on the HtmlTextView. -->
    <LinearLayout
        android:id="@+id/collapsible_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:id="@+id/title_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?selectableItemBackground"
            android:gravity="center_vertical"
            android:minHeight="?android:listPreferredItemHeightSmall"
            android:orientation="horizontal"
            android:paddingEnd="@dimen/spacing_small"
            android:paddingStart="@dimen/spacing_small">

            <ImageView
                android:id="@+id/card_title_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_done"
                android:layout_marginEnd="@dimen/spacing_small"
                android:padding="@dimen/spacing_small"
                />


            <TextView
                android:id="@+id/card_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:paddingBottom="@dimen/spacing_tiny"
                android:paddingTop="@dimen/spacing_tiny"
                android:textAlignment="viewStart"
                android:textAppearance="@style/TextAppearance.RedPass.CollapsibleCard"
                tools:text="Title" />

            <ImageView
                android:id="@+id/expand_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/spacing_small"
                android:importantForAccessibility="no"
                android:src="@drawable/ic_expand_more" />

        </LinearLayout>

        <TextView
            android:id="@+id/card_description"
            style="@style/Widget.RedPass.MultilineBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="@dimen/spacing_normal"
            android:paddingEnd="@dimen/spacing_normal"
            android:paddingStart="@dimen/spacing_normal"
            android:paddingTop="@dimen/spacing_normal"
            android:textAlignment="viewStart"
            android:textColor="?android:textColorPrimary"
            tools:text="Randdom Stuuf just ot test this"
            tools:visibility="visible" />

    </LinearLayout>

</merge>
